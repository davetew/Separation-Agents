# Domain Specific Language (`dsl`)

This module defines the data structures and schemas used to represent chemical processes and communicate between agents.

## Components

*   **`schemas.py`**: Contains Pydantic models (e.g., `Stream`, `UnitOp`, `Flowsheet`) that ensure type safety and data validation throughout the application.
