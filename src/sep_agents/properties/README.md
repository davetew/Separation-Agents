# Properties (`properties`)

This module manages the interface with thermodynamic property packages.

## Components

*   **`reaktoro.py`**: Implements the interface to the Reaktoro library, allowing agents to query thermodynamic properties (enthalpy, entropy, heat capacity) and equilibrium states. It abstracts the low-level details of the property backend.
